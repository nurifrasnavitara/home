<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Mrs. Nuri Frasna Vitara | English Teacher - SMP Muhammadiyah 9 Nagreg</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@400;500;600;700&family=Poppins:ital,wght@0,300;0,400;0,500;0,600;0,700;1,400&display=swap" rel="stylesheet">
<style>
  :root{
    --cream:#FFF8EE;
    --honey:#FFC94A;
    --coral:#FF8C7A;
    --mint:#A9DFC9;
    --sky:#9FD8E8;
    --navy:#27314F;
    --navy-soft:#4A5578;
    --gold:#E8A93C;
    --white:#FFFFFF;
    --radius-lg:28px;
    --radius-md:18px;
    --shadow-soft:0 18px 40px -18px rgba(39,49,79,0.25);
    --font-display:'Fredoka',sans-serif;
    --font-body:'Poppins',sans-serif;
  }

  *{margin:0;padding:0;box-sizing:border-box;}

  html{scroll-behavior:smooth;}

  body{
    font-family:var(--font-body);
    color:var(--navy);
    background:var(--cream);
    overflow-x:hidden;
  }

  /* ====== DECOR BLOBS ====== */
  .blob{
    position:absolute;
    border-radius:50%;
    filter:blur(0px);
    z-index:0;
    opacity:0.6;
  }

  /* ====== NAVBAR ====== */
  header{
    position:fixed;
    top:0;left:0;right:0;
    z-index:1000;
    background:rgba(255,248,238,0.85);
    backdrop-filter:blur(10px);
    border-bottom:1px solid rgba(39,49,79,0.06);
    transition:all 0.3s ease;
  }
  .nav-wrap{
    max-width:1180px;
    margin:0 auto;
    display:flex;
    align-items:center;
    justify-content:space-between;
    padding:14px 28px;
  }
  .brand{
    display:flex;
    align-items:center;
    gap:12px;
    font-family:var(--font-display);
    font-weight:600;
    font-size:1.05rem;
    color:var(--navy);
  }
  .brand img{
    height:42px;
    width:42px;
    object-fit:contain;
    border-radius:10px;
    background:var(--white);
    padding:4px;
    box-shadow:0 4px 12px rgba(39,49,79,0.12);
  }
  .brand small{
    display:block;
    font-family:var(--font-body);
    font-weight:400;
    font-size:0.68rem;
    color:var(--navy-soft);
    letter-spacing:0.04em;
  }
  nav ul{
    display:flex;
    gap:28px;
    list-style:none;
  }
  nav a{
    text-decoration:none;
    color:var(--navy-soft);
    font-weight:500;
    font-size:0.92rem;
    position:relative;
    transition:color 0.25s ease;
  }
  nav a::after{
    content:'';
    position:absolute;
    left:0;bottom:-6px;
    width:0;height:3px;
    border-radius:3px;
    background:var(--coral);
    transition:width 0.3s ease;
  }
  nav a:hover{color:var(--navy);}
  nav a:hover::after{width:100%;}

  .nav-cta{
    background:var(--navy);
    color:var(--cream);
    padding:10px 22px;
    border-radius:999px;
    font-weight:600;
    font-size:0.88rem;
    text-decoration:none;
    transition:transform 0.25s ease, background 0.25s ease;
    box-shadow:0 8px 20px -8px rgba(39,49,79,0.4);
  }
  .nav-cta:hover{
    background:var(--coral);
    transform:translateY(-2px);
  }

  .nav-toggle{display:none;background:none;border:none;font-size:1.6rem;color:var(--navy);cursor:pointer;}

  /* ====== HERO ====== */
  .hero{
    position:relative;
    min-height:100vh;
    display:flex;
    align-items:center;
    padding:120px 28px 80px;
    overflow:hidden;
  }
  .hero .blob.b1{
    width:520px;height:520px;
    background:var(--honey);
    top:-180px;right:-160px;
    opacity:0.45;
  }
  .hero .blob.b2{
    width:380px;height:380px;
    background:var(--mint);
    bottom:-140px;left:-120px;
    opacity:0.55;
  }
  .hero .blob.b3{
    width:160px;height:160px;
    background:var(--sky);
    top:18%;left:8%;
    opacity:0.45;
  }
  .hero-inner{
    position:relative;
    z-index:2;
    max-width:1180px;
    margin:0 auto;
    display:grid;
    grid-template-columns:1.15fr 0.85fr;
    gap:56px;
    align-items:center;
    width:100%;
  }
  .eyebrow{
    display:inline-flex;
    align-items:center;
    gap:8px;
    background:var(--white);
    color:var(--coral);
    font-weight:600;
    font-size:0.8rem;
    letter-spacing:0.08em;
    text-transform:uppercase;
    padding:8px 18px;
    border-radius:999px;
    box-shadow:var(--shadow-soft);
    margin-bottom:22px;
    opacity:0;
    animation:fadeUp 0.8s ease forwards;
    animation-delay:0.1s;
  }
  .hero h1{
    font-family:var(--font-display);
    font-size:clamp(2.6rem, 5.2vw, 4.2rem);
    line-height:1.08;
    font-weight:700;
    color:var(--navy);
    margin-bottom:18px;
    opacity:0;
    animation:fadeUp 0.8s ease forwards;
    animation-delay:0.25s;
  }
  .hero h1 span{
    color:var(--coral);
    position:relative;
    white-space:nowrap;
  }
  .hero h1 span svg{
    position:absolute;
    left:-4%;
    bottom:-10px;
    width:108%;
  }
  .hero p.lead{
    font-size:1.08rem;
    color:var(--navy-soft);
    max-width:480px;
    margin-bottom:34px;
    line-height:1.7;
    opacity:0;
    animation:fadeUp 0.8s ease forwards;
    animation-delay:0.4s;
  }
  .hero-buttons{
    display:flex;
    gap:14px;
    flex-wrap:wrap;
    opacity:0;
    animation:fadeUp 0.8s ease forwards;
    animation-delay:0.55s;
  }
  .btn{
    padding:15px 30px;
    border-radius:999px;
    font-weight:600;
    font-size:0.95rem;
    text-decoration:none;
    display:inline-flex;
    align-items:center;
    gap:8px;
    transition:transform 0.25s ease, box-shadow 0.25s ease;
  }
  .btn-primary{
    background:var(--coral);
    color:var(--white);
    box-shadow:0 14px 30px -12px rgba(255,140,122,0.7);
  }
  .btn-primary:hover{transform:translateY(-3px); box-shadow:0 18px 36px -12px rgba(255,140,122,0.8);}
  .btn-secondary{
    background:var(--white);
    color:var(--navy);
    box-shadow:var(--shadow-soft);
  }
  .btn-secondary:hover{transform:translateY(-3px);}

  /* portrait */
  .hero-portrait{
    position:relative;
    display:flex;
    justify-content:center;
    align-items:center;
    opacity:0;
    animation:fadeIn 1s ease forwards;
    animation-delay:0.3s;
  }
  .portrait-frame{
    position:relative;
    width:min(360px, 80vw);
    aspect-ratio:1/1.08;
    border-radius:38% 62% 60% 40% / 48% 42% 58% 52%;
    overflow:hidden;
    box-shadow:var(--shadow-soft);
    border:6px solid var(--white);
    animation:morph 12s ease-in-out infinite;
  }
  .portrait-frame img{
    width:100%;height:100%;
    object-fit:cover;
    display:block;
  }
  @keyframes morph{
    0%,100%{border-radius:38% 62% 60% 40% / 48% 42% 58% 52%;}
    50%{border-radius:54% 46% 38% 62% / 40% 58% 42% 60%;}
  }
  .floating-tag{
    position:absolute;
    background:var(--white);
    padding:10px 18px;
    border-radius:14px;
    box-shadow:var(--shadow-soft);
    font-family:var(--font-display);
    font-weight:600;
    font-size:0.92rem;
    color:var(--navy);
    display:flex;
    align-items:center;
    gap:8px;
    animation:floaty 4.5s ease-in-out infinite;
  }
  .floating-tag.tag1{ top:6%; left:-6%; animation-delay:0s;}
  .floating-tag.tag2{ bottom:10%; right:-8%; animation-delay:1.2s; background:var(--navy); color:var(--cream);}
  .floating-tag.tag3{ top:46%; right:-12%; animation-delay:2.4s;}
  @keyframes floaty{
    0%,100%{transform:translateY(0px);}
    50%{transform:translateY(-12px);}
  }
  @keyframes fadeUp{
    from{opacity:0; transform:translateY(24px);}
    to{opacity:1; transform:translateY(0);}
  }
  @keyframes fadeIn{
    from{opacity:0;}
    to{opacity:1;}
  }

  /* ====== SECTION GENERAL ====== */
  section{
    position:relative;
    padding:90px 28px;
    max-width:1180px;
    margin:0 auto;
  }
  .section-tag{
    display:inline-block;
    font-family:var(--font-display);
    font-weight:600;
    font-size:0.8rem;
    letter-spacing:0.12em;
    text-transform:uppercase;
    color:var(--gold);
    background:rgba(232,169,60,0.12);
    padding:6px 16px;
    border-radius:999px;
    margin-bottom:14px;
  }
  .section-title{
    font-family:var(--font-display);
    font-size:clamp(1.8rem, 3.4vw, 2.6rem);
    font-weight:700;
    color:var(--navy);
    margin-bottom:14px;
    max-width:640px;
  }
  .section-desc{
    color:var(--navy-soft);
    max-width:600px;
    line-height:1.7;
    font-size:1rem;
  }
  .reveal{
    opacity:0;
    transform:translateY(30px);
    transition:opacity 0.7s ease, transform 0.7s ease;
  }
  .reveal.show{
    opacity:1;
    transform:translateY(0);
  }

  /* ====== ABOUT ====== */
  .about{
    background:var(--white);
    border-radius:var(--radius-lg);
    box-shadow:var(--shadow-soft);
    padding:60px;
    display:grid;
    grid-template-columns:0.9fr 1.1fr;
    gap:50px;
    align-items:center;
    position:relative;
    overflow:hidden;
  }
  .about::before{
    content:'';
    position:absolute;
    top:-60px;right:-60px;
    width:200px;height:200px;
    background:var(--mint);
    border-radius:50%;
    opacity:0.35;
  }
  .about-photo{
    position:relative;
    border-radius:var(--radius-md);
    overflow:hidden;
    aspect-ratio:4/5;
    box-shadow:0 20px 40px -20px rgba(39,49,79,0.3);
  }
  .about-photo img{width:100%;height:100%;object-fit:cover;display:block;}
  .about-text h3{
    font-family:var(--font-display);
    font-size:1.6rem;
    margin-bottom:6px;
    color:var(--navy);
  }
  .about-text .role{
    color:var(--coral);
    font-weight:600;
    margin-bottom:18px;
    font-size:0.95rem;
  }
  .about-text p{
    color:var(--navy-soft);
    line-height:1.8;
    margin-bottom:14px;
  }
  .about-stats{
    display:flex;
    gap:28px;
    margin-top:24px;
    flex-wrap:wrap;
  }
  .about-stats div{
    background:var(--cream);
    border-radius:16px;
    padding:14px 20px;
    text-align:center;
    min-width:110px;
  }
  .about-stats strong{
    font-family:var(--font-display);
    font-size:1.5rem;
    color:var(--navy);
    display:block;
  }
  .about-stats span{
    font-size:0.78rem;
    color:var(--navy-soft);
  }

  /* ====== METHODS ====== */
  .method-grid{
    display:grid;
    grid-template-columns:repeat(4, 1fr);
    gap:22px;
    margin-top:46px;
  }
  .method-card{
    background:var(--white);
    border-radius:var(--radius-md);
    padding:30px 24px;
    box-shadow:var(--shadow-soft);
    transition:transform 0.3s ease, box-shadow 0.3s ease;
    border-top:4px solid var(--honey);
  }
  .method-card:nth-child(2){border-top-color:var(--coral);}
  .method-card:nth-child(3){border-top-color:var(--sky);}
  .method-card:nth-child(4){border-top-color:var(--mint);}
  .method-card:hover{
    transform:translateY(-8px);
    box-shadow:0 24px 50px -22px rgba(39,49,79,0.3);
  }
  .method-icon{
    width:54px;height:54px;
    border-radius:14px;
    display:flex;align-items:center;justify-content:center;
    margin-bottom:18px;
    background:var(--cream);
    font-size:1.6rem;
  }
  .method-card h4{
    font-family:var(--font-display);
    font-size:1.1rem;
    margin-bottom:8px;
    color:var(--navy);
  }
  .method-card p{
    color:var(--navy-soft);
    font-size:0.92rem;
    line-height:1.65;
  }

  /* ====== MATERI / KELAS ====== */
  .class-grid{
    display:grid;
    grid-template-columns:repeat(3, 1fr);
    gap:24px;
    margin-top:46px;
  }
  .class-card{
    position:relative;
    border-radius:var(--radius-md);
    padding:34px 28px;
    color:var(--white);
    overflow:hidden;
    min-height:230px;
    display:flex;
    flex-direction:column;
    justify-content:space-between;
    box-shadow:var(--shadow-soft);
  }
  .class-card.c1{background:linear-gradient(145deg, #FF8C7A, #FFB199);}
  .class-card.c2{background:linear-gradient(145deg, #4A5578, #27314F);}
  .class-card.c3{background:linear-gradient(145deg, #5BC0B7, #9FD8E8);}
  .class-card .grade{
    font-family:var(--font-display);
    font-size:2.6rem;
    font-weight:700;
    opacity:0.85;
  }
  .class-card h4{
    font-family:var(--font-display);
    font-size:1.25rem;
    margin-bottom:8px;
  }
  .class-card p{
    font-size:0.9rem;
    opacity:0.92;
    line-height:1.6;
  }
  .class-card ul{
    list-style:none;
    margin-top:10px;
    font-size:0.86rem;
    opacity:0.95;
    line-height:1.9;
  }
  .class-card ul li::before{
    content:'✦ ';
  }

  /* ====== GALERI ====== */
  .gallery-grid{
    display:grid;
    grid-template-columns:repeat(4, 1fr);
    grid-template-rows:repeat(2, 160px);
    gap:18px;
    margin-top:46px;
  }
  .gallery-grid .g-item{
    border-radius:var(--radius-md);
    overflow:hidden;
    position:relative;
    box-shadow:var(--shadow-soft);
  }
  .gallery-grid .g-item img{
    width:100%;height:100%;object-fit:cover;display:block;
    transition:transform 0.5s ease;
  }
  .gallery-grid .g-item:hover img{transform:scale(1.08);}
  .gallery-grid .g1{grid-column:1/3; grid-row:1/3;}
  .gallery-grid .g2{grid-column:3/5; grid-row:1/2;}
  .gallery-grid .g3{grid-column:3/4; grid-row:2/3;}
  .gallery-grid .g4{grid-column:4/5; grid-row:2/3;}
  .g-caption{
    position:absolute;
    bottom:0;left:0;right:0;
    padding:16px;
    background:linear-gradient(to top, rgba(39,49,79,0.75), transparent);
    color:var(--white);
    font-weight:600;
    font-size:0.9rem;
  }

  /* ====== TESTIMONI ====== */
  .testimoni-wrap{
    margin-top:46px;
    position:relative;
  }
  .testi-track{
    display:flex;
    gap:24px;
    overflow-x:auto;
    scroll-snap-type:x mandatory;
    padding-bottom:10px;
    scrollbar-width:none;
  }
  .testi-track::-webkit-scrollbar{display:none;}
  .testi-card{
    flex:0 0 320px;
    scroll-snap-align:start;
    background:var(--white);
    border-radius:var(--radius-md);
    padding:30px;
    box-shadow:var(--shadow-soft);
    position:relative;
  }
  .testi-quote{
    font-size:2.4rem;
    color:var(--honey);
    font-family:var(--font-display);
    line-height:1;
    margin-bottom:10px;
  }
  .testi-card p.text{
    color:var(--navy-soft);
    font-size:0.95rem;
    line-height:1.7;
    margin-bottom:20px;
    font-style:italic;
  }
  .testi-author{
    display:flex;
    align-items:center;
    gap:12px;
  }
  .testi-avatar{
    width:44px;height:44px;
    border-radius:50%;
    background:var(--mint);
    display:flex;align-items:center;justify-content:center;
    font-family:var(--font-display);
    font-weight:600;
    color:var(--navy);
  }
  .testi-author strong{display:block; font-size:0.92rem; color:var(--navy);}
  .testi-author span{font-size:0.78rem; color:var(--navy-soft);}

  /* ====== CONTACT ====== */
  .contact-box{
    background:var(--navy);
    border-radius:var(--radius-lg);
    padding:60px;
    color:var(--cream);
    display:grid;
    grid-template-columns:1fr 1fr;
    gap:50px;
    position:relative;
    overflow:hidden;
  }
  .contact-box::before{
    content:'';
    position:absolute;
    bottom:-100px;right:-80px;
    width:260px;height:260px;
    border-radius:50%;
    background:var(--honey);
    opacity:0.15;
  }
  .contact-info .section-tag{
    background:rgba(255,255,255,0.08);
    color:var(--honey);
  }
  .contact-info h2{
    font-family:var(--font-display);
    font-size:clamp(1.8rem,3vw,2.4rem);
    margin-bottom:14px;
  }
  .contact-info p{
    color:#C9CEE0;
    line-height:1.8;
    margin-bottom:28px;
    max-width:420px;
  }
  .contact-list{
    display:flex;
    flex-direction:column;
    gap:14px;
  }
  .contact-list a, .contact-list div{
    display:flex;
    align-items:center;
    gap:12px;
    color:var(--cream);
    text-decoration:none;
    font-size:0.95rem;
    font-weight:500;
  }
  .contact-list .ic{
    width:38px;height:38px;
    border-radius:10px;
    background:rgba(255,255,255,0.08);
    display:flex;align-items:center;justify-content:center;
    font-size:1.1rem;
  }
  form.contact-form{
    background:rgba(255,255,255,0.05);
    border-radius:var(--radius-md);
    padding:30px;
    display:flex;
    flex-direction:column;
    gap:16px;
    border:1px solid rgba(255,255,255,0.08);
  }
  form.contact-form input,
  form.contact-form textarea{
    background:rgba(255,255,255,0.07);
    border:1px solid rgba(255,255,255,0.12);
    border-radius:12px;
    padding:13px 16px;
    color:var(--cream);
    font-family:var(--font-body);
    font-size:0.92rem;
    outline:none;
    transition:border-color 0.25s ease;
  }
  form.contact-form input::placeholder,
  form.contact-form textarea::placeholder{color:#8A93B5;}
  form.contact-form input:focus,
  form.contact-form textarea:focus{border-color:var(--honey);}
  form.contact-form textarea{resize:vertical; min-height:90px;}
  form.contact-form button{
    background:var(--coral);
    color:var(--white);
    border:none;
    padding:14px;
    border-radius:12px;
    font-weight:600;
    font-size:0.95rem;
    cursor:pointer;
    font-family:var(--font-body);
    transition:background 0.25s ease, transform 0.25s ease;
  }
  form.contact-form button:hover{background:var(--honey); color:var(--navy); transform:translateY(-2px);}
  .form-msg{font-size:0.85rem; color:var(--mint); display:none;}

  /* ====== FOOTER ====== */
  footer{
    padding:36px 28px;
    text-align:center;
    color:var(--navy-soft);
    font-size:0.85rem;
  }
  footer .foot-brand{
    display:flex;
    align-items:center;
    justify-content:center;
    gap:10px;
    margin-bottom:10px;
    font-family:var(--font-display);
    font-weight:600;
    color:var(--navy);
  }
  footer .foot-brand img{height:32px; border-radius:8px;}

  /* ====== FLOATING WHATSAPP ====== */
  .wa-float{
    position:fixed;
    bottom:26px; right:26px;
    width:58px; height:58px;
    background:#2EBE6A;
    border-radius:50%;
    display:flex;align-items:center;justify-content:center;
    box-shadow:0 14px 30px -10px rgba(46,190,106,0.6);
    z-index:999;
    text-decoration:none;
    animation:pulse 2.4s ease-in-out infinite;
  }
  .wa-float svg{width:28px;height:28px;fill:#fff;}
  @keyframes pulse{
    0%,100%{box-shadow:0 14px 30px -10px rgba(46,190,106,0.6);}
    50%{box-shadow:0 14px 30px -2px rgba(46,190,106,0.85);}
  }

  /* ====== RESPONSIVE ====== */
  @media (max-width: 980px){
    nav ul, .nav-cta{display:none;}
    .nav-toggle{display:block;}
    .hero-inner{grid-template-columns:1fr; text-align:center;}
    .hero p.lead{margin-inline:auto;}
    .hero-buttons{justify-content:center;}
    .hero-portrait{order:-1;}
    .about{grid-template-columns:1fr; padding:36px;}
    .method-grid{grid-template-columns:repeat(2,1fr);}
    .class-grid{grid-template-columns:1fr;}
    .gallery-grid{grid-template-columns:repeat(2,1fr); grid-template-rows:repeat(3,160px);}
    .gallery-grid .g1{grid-column:1/3; grid-row:1/2;}
    .gallery-grid .g2{grid-column:1/2; grid-row:2/3;}
    .gallery-grid .g3{grid-column:2/3; grid-row:2/3;}
    .gallery-grid .g4{grid-column:1/3; grid-row:3/4;}
    .contact-box{grid-template-columns:1fr; padding:36px;}
  }
  @media (max-width: 560px){
    .method-grid{grid-template-columns:1fr;}
    .floating-tag{display:none;}
    section{padding:70px 18px;}
    .about{padding:26px;}
    .contact-box{padding:26px;}
  }

  @media (prefers-reduced-motion: reduce){
    *{animation:none !important; transition:none !important;}
  }
</style>
</head>
<body>

<!-- ===== HEADER ===== -->
<header>
  <div class="nav-wrap">
    <div class="brand">
      <img src="https://i.ibb.co.com/5Xb4Crkq/Logo-Sekolah-01.png" alt="Logo SMP Muhammadiyah 9 Nagreg">
      <div>
        Mrs. Nuri Frasna Vitara
        <small>SMP Muhammadiyah 9 Nagreg</small>
      </div>
    </div>
    <nav>
      <ul>
        <li><a href="#about">Tentang</a></li>
        <li><a href="#methods">Metode</a></li>
        <li><a href="#classes">Kelas</a></li>
        <li><a href="#gallery">Galeri</a></li>
        <li><a href="#testimoni">Testimoni</a></li>
      </ul>
    </nav>
    <a href="#contact" class="nav-cta">Hubungi Saya</a>
    <button class="nav-toggle" aria-label="menu">☰</button>
  </div>
</header>

<!-- ===== HERO ===== -->
<section class="hero">
  <div class="blob b1"></div>
  <div class="blob b2"></div>
  <div class="blob b3"></div>
  <div class="hero-inner">
    <div>
      <span class="eyebrow">👋 Hello, Students!</span>
      <h1>Belajar Bahasa Inggris jadi
        <span>seru &amp; bermakna
          <svg viewBox="0 0 300 20" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M2 14 C 60 4, 240 4, 298 14" stroke="#FFC94A" stroke-width="6" fill="none" stroke-linecap="round"/>
          </svg>
        </span>
        bersama Mrs. Nuri
      </h1>
      <p class="lead">
        English Teacher di SMP Muhammadiyah 9 Nagreg. Mengajak siswa untuk percaya diri berbicara, menulis, dan berpikir dalam Bahasa Inggris melalui metode yang menyenangkan, kreatif, dan penuh semangat.
      </p>
      <div class="hero-buttons">
        <a href="#classes" class="btn btn-primary">📚 Lihat Kelas</a>
        <a href="#contact" class="btn btn-secondary">💬 Hubungi Saya</a>
      </div>
    </div>
    <div class="hero-portrait">
      <div class="portrait-frame">
        <img src="https://lh3.googleusercontent.com/d/10lRsML_DXew3x1mjPwGt2_c3mfZLp7ub" alt="Foto Mrs. Nuri Frasna Vitara">
      </div>
      <div class="floating-tag tag1">✏️ Grammar Fun</div>
      <div class="floating-tag tag2">🌍 Speak Up!</div>
      <div class="floating-tag tag3">📖 Storytelling</div>
    </div>
  </div>
</section>

<!-- ===== ABOUT ===== -->
<section id="about">
  <div class="about reveal">
    <div class="about-photo">
      <img src="https://lh3.googleusercontent.com/d/10lRsML_DXew3x1mjPwGt2_c3mfZLp7ub" alt="Mrs. Nuri Frasna Vitara">
    </div>
    <div class="about-text">
      <span class="section-tag">Tentang Saya</span>
      <h3>Mrs. Nuri Frasna Vitara</h3>
      <div class="role">English Teacher — SMP Muhammadiyah 9 Nagreg</div>
      <p>
        Assalamu'alaikum! Saya Mrs. Nuri, guru Bahasa Inggris yang percaya bahwa setiap siswa bisa menguasai bahasa asing dengan cara yang menyenangkan, bukan menakutkan. "Learning English should feel like an adventure, not a burden."
      </p>
      <p>
        Dengan pengalaman mengajar di jenjang SMP, saya fokus membangun kepercayaan diri siswa melalui speaking practice, games interaktif, storytelling, dan tugas kreatif yang relevan dengan kehidupan sehari-hari mereka.
      </p>
      <div class="about-stats">
        <div>
          <strong>3+</strong>
          <span>Tahun Mengajar</span>
        </div>
        <div>
          <strong>4</strong>
          <span>Metode Belajar</span>
        </div>
        <div>
          <strong>7-9</strong>
          <span>Jenjang Kelas</span>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ===== METHODS ===== -->
<section id="methods">
  <div class="reveal">
    <span class="section-tag">Teaching Approach</span>
    <h2 class="section-title">Metode mengajar yang ceria dan efektif</h2>
    <p class="section-desc">Setiap siswa memiliki cara belajar yang berbeda — karena itu Mrs. Nuri menggabungkan beberapa metode interaktif agar Bahasa Inggris terasa hidup di kelas.</p>
  </div>
  <div class="method-grid">
    <div class="method-card reveal">
      <div class="method-icon">🗣️</div>
      <h4>Speaking Practice</h4>
      <p>Latihan berbicara melalui role-play, diskusi, dan presentasi singkat agar siswa berani speak up tanpa takut salah.</p>
    </div>
    <div class="method-card reveal">
      <div class="method-icon">🎮</div>
      <h4>Games &amp; Quiz</h4>
      <p>Belajar vocabulary dan grammar lewat games interaktif, kuis seru, dan tantangan kelompok yang membuat suasana kelas hidup.</p>
    </div>
    <div class="method-card reveal">
      <div class="method-icon">📖</div>
      <h4>Storytelling</h4>
      <p>Mengasah listening dan reading comprehension melalui cerita pendek, dongeng, dan video edukatif berbahasa Inggris.</p>
    </div>
    <div class="method-card reveal">
      <div class="method-icon">✏️</div>
      <h4>Grammar Fun</h4>
      <p>Memahami tenses dan struktur kalimat dengan trik mudah diingat, contoh nyata, dan latihan menulis kreatif.</p>
    </div>
  </div>
</section>

<!-- ===== CLASSES ===== -->
<section id="classes">
  <div class="reveal">
    <span class="section-tag">Materi &amp; Kelas</span>
    <h2 class="section-title">Materi pembelajaran untuk kelas 7-9</h2>
    <p class="section-desc">Kurikulum disusun bertahap sesuai jenjang, membangun kemampuan dasar hingga komunikasi yang lebih kompleks.</p>
  </div>
  <div class="class-grid">
    <div class="class-card c1 reveal">
      <div class="grade">VII</div>
      <div>
        <h4>Foundation English</h4>
        <p>Membangun dasar kosakata, ucapan salam, dan kalimat sederhana sehari-hari.</p>
        <ul>
          <li>Introduction &amp; Greetings</li>
          <li>Simple Present Tense</li>
          <li>Daily Vocabulary</li>
        </ul>
      </div>
    </div>
    <div class="class-card c2 reveal">
      <div class="grade">VIII</div>
      <div>
        <h4>Communicative English</h4>
        <p>Mengembangkan kemampuan berbicara dan menulis melalui teks fungsional.</p>
        <ul>
          <li>Descriptive &amp; Narrative Text</li>
          <li>Past Tense in Stories</li>
          <li>Group Discussion</li>
        </ul>
      </div>
    </div>
    <div class="class-card c3 reveal">
      <div class="grade">IX</div>
      <div>
        <h4>Advanced &amp; Exam Prep</h4>
        <p>Mempersiapkan siswa menghadapi ujian dengan latihan soal dan teks lebih kompleks.</p>
        <ul>
          <li>Procedure &amp; Report Text</li>
          <li>Reading Comprehension</li>
          <li>Mock Test &amp; Review</li>
        </ul>
      </div>
    </div>
  </div>
</section>

<!-- ===== GALLERY ===== -->
<section id="gallery">
  <div class="reveal">
    <span class="section-tag">Galeri Kegiatan</span>
    <h2 class="section-title">Momen seru di kelas Bahasa Inggris</h2>
    <p class="section-desc">Dokumentasi suasana belajar, aktivitas kelompok, dan kreativitas siswa selama pembelajaran.</p>
  </div>
  <div class="gallery-grid reveal">
    <div class="g-item g1">
      <img src="https://images.unsplash.com/photo-1580582932707-520aed937b7b?q=80&w=1200&auto=format&fit=crop" alt="Diskusi kelompok siswa">
      <div class="g-caption">Group Discussion Activity</div>
    </div>
    <div class="g-item g2">
      <img src="https://images.unsplash.com/photo-1503676260728-1c00da094a0b?q=80&w=1200&auto=format&fit=crop" alt="Kegiatan presentasi kelas">
      <div class="g-caption">Speaking Practice</div>
    </div>
    <div class="g-item g3">
      <img src="https://images.unsplash.com/photo-1456513080510-7bf3a84b82f8?q=80&w=1200&auto=format&fit=crop" alt="Belajar dengan buku">
      <div class="g-caption">Reading Time</div>
    </div>
    <div class="g-item g4">
      <img src="https://images.unsplash.com/photo-1571260899304-425eee4c7efc?q=80&w=1200&auto=format&fit=crop" alt="Suasana kelas ceria">
      <div class="g-caption">Classroom Vibes</div>
    </div>
  </div>
</section>

<!-- ===== TESTIMONI ===== -->
<section id="testimoni">
  <div class="reveal">
    <span class="section-tag">Testimoni Siswa</span>
    <h2 class="section-title">Apa kata mereka tentang kelas Mrs. Nuri?</h2>
    <p class="section-desc">Suara langsung dari siswa-siswi SMP Muhammadiyah 9 Nagreg.</p>
  </div>
  <div class="testimoni-wrap reveal">
    <div class="testi-track">
      <div class="testi-card">
        <div class="testi-quote">"</div>
        <p class="text">Awalnya saya takut belajar Bahasa Inggris, tapi setelah diajar Mrs. Nuri jadi lebih percaya diri buat ngomong di depan kelas.</p>
        <div class="testi-author">
          <div class="testi-avatar">RA</div>
          <div>
            <strong>Rifqi A.</strong>
            <span>Siswa Kelas VIII</span>
          </div>
        </div>
      </div>
      <div class="testi-card">
        <div class="testi-quote">"</div>
        <p class="text">Belajar grammar jadi gampang banget karena ada triknya. Games di kelas juga bikin nggak ngantuk!</p>
        <div class="testi-author">
          <div class="testi-avatar">SN</div>
          <div>
            <strong>Salma N.</strong>
            <span>Siswi Kelas VII</span>
          </div>
        </div>
      </div>
      <div class="testi-card">
        <div class="testi-quote">"</div>
        <p class="text">Suka banget sama sesi storytelling-nya, jadi lebih paham cara baca teks Bahasa Inggris dan artinya.</p>
        <div class="testi-author">
          <div class="testi-avatar">DF</div>
          <div>
            <strong>Dimas F.</strong>
            <span>Siswa Kelas IX</span>
          </div>
        </div>
      </div>
      <div class="testi-card">
        <div class="testi-quote">"</div>
        <p class="text">Mrs. Nuri sabar banget kalau ngajarin, dan selalu kasih semangat ke murid-muridnya.</p>
        <div class="testi-author">
          <div class="testi-avatar">AK</div>
          <div>
            <strong>Aulia K.</strong>
            <span>Siswi Kelas VIII</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ===== CONTACT ===== -->
<section id="contact">
  <div class="contact-box reveal">
    <div class="contact-info">
      <span class="section-tag">Hubungi Saya</span>
      <h2>Punya pertanyaan seputar pelajaran?</h2>
      <p>Jangan ragu untuk menghubungi Mrs. Nuri melalui WhatsApp, email, atau kunjungi langsung sekolah. Let's make English learning fun together!</p>
      <div class="contact-list">
        <a href="https://wa.me/6285793259563" target="_blank" rel="noopener">
          <span class="ic">💬</span> +62 857-9325-9563 (WhatsApp)
        </a>
        <a href="mailto:nuri.frasna@smpmuh9nagreg.sch.id">
          <span class="ic">✉️</span> nuri.frasna@smpmuh9nagreg.sch.id
        </a>
        <div>
          <span class="ic">📍</span> SMP Muhammadiyah 9 Nagreg, Kabupaten Bandung, Jawa Barat
        </div>
      </div>
    </div>
    <form class="contact-form" id="contactForm">
      <input type="text" placeholder="Nama lengkap" required>
      <input type="email" placeholder="Alamat email" required>
      <textarea placeholder="Tulis pertanyaan atau pesan Anda..." required></textarea>
      <button type="submit">Kirim Pesan</button>
      <span class="form-msg" id="formMsg">Terima kasih! Pesan Anda telah terkirim. 🎉</span>
    </form>
  </div>
</section>

<!-- ===== FOOTER ===== -->
<footer>
  <div class="foot-brand">
    <img src="https://i.ibb.co.com/5Xb4Crkq/Logo-Sekolah-01.png" alt="Logo Sekolah">
    SMP Muhammadiyah 9 Nagreg
  </div>
  <p>&copy; 2026 Mrs. Nuri Frasna Vitara — English Teacher. All rights reserved.</p>
</footer>

<!-- ===== FLOATING WHATSAPP ===== -->
<a href="https://wa.me/6285793259563" class="wa-float" target="_blank" rel="noopener" aria-label="Chat WhatsApp">
  <svg viewBox="0 0 32 32" xmlns="http://www.w3.org/2000/svg"><path d="M16.001 3C9.373 3 4 8.373 4 15.001c0 2.566.806 4.945 2.179 6.9L4.5 28.5l6.78-1.78A11.93 11.93 0 0 0 16.001 27C22.629 27 28 21.628 28 15.001 28 8.373 22.629 3 16.001 3zm6.97 16.86c-.293.823-1.45 1.586-1.998 1.673-.512.082-1.16.116-1.873-.117-.43-.139-.985-.323-1.7-.633-2.992-1.293-4.943-4.305-5.09-4.504-.147-.198-1.214-1.615-1.214-3.08 0-1.465.77-2.184 1.044-2.485.273-.302.598-.378.797-.378.198 0 .397.002.57.01.183.009.428-.07.67.51.252.604.854 2.085.93 2.236.075.151.126.328.025.527-.1.198-.151.323-.298.498-.149.174-.314.39-.448.524-.149.149-.305.31-.13.61.174.298.776 1.28 1.665 2.075 1.143 1.022 2.108 1.34 2.41 1.49.302.149.477.124.652-.075.174-.198.747-.873.946-1.175.198-.302.397-.252.67-.149.273.1 1.725.813 2.024.96.298.149.497.224.57.348.075.124.075.722-.218 1.546z"/></svg>
</a>

<script>
  // Reveal on scroll
  const revealEls = document.querySelectorAll('.reveal');
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if(entry.isIntersecting){
        entry.target.classList.add('show');
        observer.unobserve(entry.target);
      }
    });
  }, { threshold: 0.15 });
  revealEls.forEach(el => observer.observe(el));

  // Header shadow on scroll
  const header = document.querySelector('header');
  window.addEventListener('scroll', () => {
    if(window.scrollY > 20){
      header.style.boxShadow = '0 6px 24px -16px rgba(39,49,79,0.4)';
    } else {
      header.style.boxShadow = 'none';
    }
  });

  // Contact form (demo)
  const form = document.getElementById('contactForm');
  const msg = document.getElementById('formMsg');
  form.addEventListener('submit', (e) => {
    e.preventDefault();
    msg.style.display = 'block';
    form.reset();
    setTimeout(() => msg.style.display = 'none', 4000);
  });

  // Mobile nav toggle (simple smooth-scroll fallback already handled via anchor links)
  const toggle = document.querySelector('.nav-toggle');
  const navUl = document.querySelector('nav ul');
  toggle.addEventListener('click', () => {
    navUl.style.display = navUl.style.display === 'flex' ? 'none' : 'flex';
    navUl.style.flexDirection = 'column';
    navUl.style.position = 'absolute';
    navUl.style.top = '70px';
    navUl.style.right = '20px';
    navUl.style.background = '#fff';
    navUl.style.padding = '20px 28px';
    navUl.style.borderRadius = '16px';
    navUl.style.boxShadow = '0 18px 40px -18px rgba(39,49,79,0.25)';
    navUl.style.gap = '14px';
  });
</script>

</body>
</html>
